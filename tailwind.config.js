/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        './src/**/*.{html,js,ts,jsx,tsx}', // Example content paths
        './public/index.html',
    ],
    theme: {
        extend: {
            // Extend the default theme with custom values
        },
    },
    plugins: [],
};
